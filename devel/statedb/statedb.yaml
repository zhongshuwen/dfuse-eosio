start:
  args:
  - blockmeta
  - booter
  - statedb
  - merger
  - mindreader
  - node-manager
  - relayer
  flags:
    common-exclude-filter-expr: (receiver == "zswhq" && action == "onblock")
    statedb-grpc-listen-addr: :9000
    statedb-http-listen-addr: :8080